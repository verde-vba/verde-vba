# Dependencies
node_modules/
mcp/node_modules/

# Build output
dist/
src-tauri/target/
*.exe
*.msi

# Logs
*.log

# IDE
.vscode/
.idea/
*.sw?

# OS
.DS_Store
Thumbs.db

# Nix
result

# Local env
*.local

# Tree-sitter WASM artifact (fetched from treesitter-vba GitHub Releases via
# `just fetch-wasm`; release-cycle-independent — never commit).
public/tree-sitter-vba.wasm

# verde-lsp sidecar binaries (fetched from verde-lsp GitHub Releases via
# `just fetch-lsp` in Sprint 32.E; release-cycle-independent — never commit).
# `.gitkeep` sentinel preserves the directory itself.
src-tauri/binaries/verde-lsp*


# Added by cargo

/target

# Guard against stray root-level Rust scaffolding.
# The real Rust crate lives at src-tauri/Cargo.toml; src/ is the React app.
/Cargo.toml
/Cargo.lock
/src/main.rs
